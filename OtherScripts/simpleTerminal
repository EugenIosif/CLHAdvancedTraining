import serial
import time
import random

ser = serial.Serial(
    port='COM9',
    baudrate=115200,
    parity=serial.PARITY_NONE,
    stopbits=serial.STOPBITS_ONE,
    bytesize=serial.EIGHTBITS
)

while True:
    if(ser.in_waiting > 0):
        bs = ser.read(ser.in_waiting)  # Clear the buffer
        # bs = ser.readline()
        # public_exponent = bs[0:7]
        # modulus = bs[8:15]
        print(bs.hex())

        # print("public exponent: ", public_exponent.hex())
        # print("modulus: ", modulus.hex())
        # time.sleep(1)
    
